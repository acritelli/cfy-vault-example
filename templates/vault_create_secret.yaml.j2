rest_calls:
  - path: /v1/kv-v2/data/{{ secret_name }}
    response_format: json
    method: PUT
    recoverable_codes: []
    headers:
      X-Vault-Token: {{ token }}
    payload:
      data:
        {{ secret_data | tojson }} 
